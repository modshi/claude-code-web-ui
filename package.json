{
  "name": "claude-code-web-ui",
  "description": "A web-based UI for Claude Code CLI — because the terminal isn't always enough",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "lucide": "^0.577.0",
    "ws": "^8.18.0"
  }
}
